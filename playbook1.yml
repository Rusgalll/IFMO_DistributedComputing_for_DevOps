---
- name: Установка Docker и запуск WordPress
  hosts: carrier
  become: true

  vars_files:
    - vars/wordpress.yml

  tasks:
    - name: Подготовка Docker
      ansible.builtin.import_tasks: tasks/docker/docker.yml

    - name: Подготовка MySQL
      ansible.builtin.import_tasks: tasks/mysql/mysql.yml

    - name: Подготовка WordPress
      ansible.builtin.import_tasks: tasks/wordpress/wordpress.yml
